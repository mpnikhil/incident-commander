-- CreateTable
CREATE TABLE "Example" (
    "exampleId" TEXT NOT NULL PRIMARY KEY,
    "createdAtNowLocaltime" TIMESTAMP NOT NULL DEFAULT (datetime('now','localtime')),
    "updatedAtNowLocaltime" TIMESTAMP NOT NULL DEFAULT (datetime('now','localtime')),
    "createdAtNowUTC" TIMESTAMP NOT NULL DEFAULT (datetime('now','utc')),
    "updatedAtNowUTC" TIMESTAMP NOT NULL DEFAULT (datetime('now','utc')),
    "createdAtCurrentTimestamp" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAtCurrentTimestamp" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "createdAtStrftime" TIMESTAMP NOT NULL DEFAULT (strftime('%FT%R:%fZ', 'now')),
    "updatedAtStrftime" TIMESTAMP NOT NULL DEFAULT (strftime('%FT%R:%fZ', 'now'))
);
