application "nightrider-mvp" {
  # Just the main service for now
  service "sre-agent" {
    visibility = "public"
    domain {
      cname = "nightrider-sre"
    }
  }

  # SQL database
  sql_database "incidents-db" {}

  # One simple MCP service for testing
  mcp_service "mapping-mcp" {
    visibility = "protected"
    authorization_server = "https://authkit.liquidmetal.run"
  }
}